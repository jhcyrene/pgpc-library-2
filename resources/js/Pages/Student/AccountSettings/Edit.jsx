import { useForm, usePage } from '@inertiajs/react';
import { useState } from 'react';

import StudentLayout from '../../../Layouts/StudentLayout';

export default function Edit({ account, form: formConfig }) {
    const { flash } = usePage().props;
    const form = useForm({ current_password: '', password: '', password_confirmation: '' });
    const [visible, setVisible] = useState(false);
    const submit = (event) => {
        event.preventDefault();
        form.put(formConfig.updatePasswordUrl, { preserveScroll: true, onSuccess: () => form.reset() });
    };

    return <StudentLayout title="Account Settings" active="settings"><div className="mx-auto max-w-3xl space-y-5"><section className="rounded-2xl bg-[#102b70] p-6 text-white shadow-md"><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-200">Student account</p><h1 className="mt-2 text-3xl font-black">Account settings</h1><p className="mt-2 text-sm text-slate-300">Manage your password and review account security information.</p></section>{flash?.success && <div role="status" className="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{flash.success}</div>}{flash?.error && <div role="alert" className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm font-bold text-red-700">{flash.error}</div>}<section className="grid gap-5 md:grid-cols-[minmax(0,1.6fr)_minmax(230px,0.8fr)]"><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><div className="mb-6"><h2 className="text-xl font-extrabold text-slate-900">Change password</h2><p className="mt-1 text-sm text-slate-500">Confirm your current password before choosing a new one.</p></div><div className="space-y-5"><PasswordField label="Current password" name="current_password" value={form.data.current_password} error={form.errors.current_password} visible={visible} onChange={(value) => form.setData('current_password', value)} autoComplete="current-password" /><PasswordField label="New password" name="password" value={form.data.password} error={form.errors.password} visible={visible} onChange={(value) => form.setData('password', value)} autoComplete="new-password" /><PasswordField label="Confirm new password" name="password_confirmation" value={form.data.password_confirmation} visible={visible} onChange={(value) => form.setData('password_confirmation', value)} autoComplete="new-password" /></div><div className="mt-4 flex items-center justify-between gap-4"><label className="flex cursor-pointer items-center gap-2 text-xs font-semibold text-slate-500"><input type="checkbox" checked={visible} onChange={(event) => setVisible(event.target.checked)} className="rounded border-slate-300" />Show passwords</label><span className="text-xs text-slate-400">Use at least 8 characters.</span></div><div className="mt-7 flex justify-end border-t border-slate-100 pt-6"><button type="submit" disabled={form.processing} className="rounded-xl bg-[#102b70] px-6 py-3 text-sm font-bold text-white disabled:opacity-60">{form.processing ? 'Updating…' : 'Update password'}</button></div></form><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-extrabold uppercase tracking-wide text-slate-700">Account overview</h2><dl className="mt-5 space-y-4"><Detail label="Username" value={account.username} /><Detail label="Account type" value={account.accountType} /><Detail label="Status" value={account.accountStatus} status /><Detail label="Last login" value={account.lastLogin || 'Not recorded'} /><Detail label="Password changed" value={account.passwordChangedAt || 'Not recorded'} /></dl><div className="mt-6 rounded-xl border border-blue-100 bg-blue-50 p-3 text-xs leading-relaxed text-blue-800">The library will never ask you to share your password by email or message.</div></aside></section></div></StudentLayout>;
}

function PasswordField({ label, name, value, error, visible, onChange, autoComplete }) { return <div><label htmlFor={name} className="mb-1.5 block text-sm font-bold text-slate-700">{label}</label><input id={name} type={visible ? 'text' : 'password'} value={value} onChange={(event) => onChange(event.target.value)} autoComplete={autoComplete} required minLength={name === 'current_password' ? undefined : 8} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 text-sm outline-none focus:border-[#102b70]" />{error && <p className="mt-1 text-xs font-semibold text-red-600">{error}</p>}</div>; }
function Detail({ label, value, status = false }) { return <div><dt className="text-[10px] font-bold uppercase tracking-wider text-slate-400">{label}</dt><dd className={`mt-1 text-sm font-bold ${status ? 'text-emerald-700' : 'text-slate-800'}`}>{value || '—'}</dd></div>; }
