import { useForm, usePage } from '@inertiajs/react';

import StudentLayout from '../../../Layouts/StudentLayout';
import { BookCover } from '../../../components/student/ReservationStatus';

export default function Create({ book, eligibility, form }) {
    const { studentPortal } = usePage().props;
    const reservation = useForm({ pickup_date: form.minimumPickupDate, remarks: '' });
    const submit = (event) => { event.preventDefault(); reservation.post(form.storeUrl); };

    return <StudentLayout title="Reserve Book" active="reservations"><div className="mx-auto max-w-3xl space-y-5"><a href={studentPortal.routes.catalog} className="inline-flex text-sm font-bold text-slate-500 hover:text-[#102b70]">← Back to catalog</a><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 bg-[#102b70] p-6 text-white"><p className="text-xs font-bold uppercase tracking-wider text-blue-200">Reservation request</p><h1 className="mt-2 text-2xl font-black">Confirm reservation</h1></div><div className="space-y-7 p-6 md:p-8"><div className="flex flex-col gap-5 rounded-xl border border-slate-200 bg-slate-50 p-5 sm:flex-row"><BookCover book={book} className="mx-auto h-36 w-24 sm:mx-0" /><div className="text-center sm:text-left"><h2 className="text-xl font-extrabold text-slate-900">{book.title}</h2><p className="mt-1 text-sm text-slate-500">{book.authors.length ? `by ${book.authors.join(', ')}` : 'Unknown author'}</p><dl className="mt-5 grid grid-cols-2 gap-5"><Detail label="ISBN" value={book.isbn} /><Detail label="Publication year" value={book.publicationYear} /></dl></div></div>{eligibility.eligible ? <form onSubmit={submit} className="space-y-6"><div><label htmlFor="pickup_date" className="mb-1.5 block text-sm font-bold text-slate-700">Preferred pickup date</label><input id="pickup_date" type="date" min={form.minimumPickupDate} value={reservation.data.pickup_date} onChange={(event) => reservation.setData('pickup_date', event.target.value)} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 text-sm outline-none focus:border-[#102b70]" required />{reservation.errors.pickup_date && <p className="mt-1 text-xs font-semibold text-red-600">{reservation.errors.pickup_date}</p>}</div><div><label htmlFor="remarks" className="mb-1.5 block text-sm font-bold text-slate-700">Remarks / notes <span className="font-normal text-slate-400">(optional)</span></label><textarea id="remarks" rows="4" maxLength="500" value={reservation.data.remarks} onChange={(event) => reservation.setData('remarks', event.target.value)} className="w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm outline-none focus:border-[#102b70]" placeholder="Any special notes for the librarian…" />{reservation.errors.remarks && <p className="mt-1 text-xs font-semibold text-red-600">{reservation.errors.remarks}</p>}</div><div className="rounded-xl border border-blue-100 bg-blue-50 p-4 text-sm leading-relaxed text-blue-800">The library will prepare the book after reviewing your request. Monitor your dashboard for status updates and pickup instructions.</div><div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href={studentPortal.routes.catalog} className="rounded-xl px-5 py-3 text-center text-sm font-bold text-slate-600 hover:bg-slate-100">Cancel</a><button type="submit" disabled={reservation.processing} className="rounded-xl bg-[#102b70] px-6 py-3 text-sm font-bold text-white disabled:opacity-60">{reservation.processing ? 'Submitting…' : 'Confirm reservation'}</button></div></form> : <div className="rounded-xl border border-red-200 bg-red-50 p-6 text-center"><div className="text-4xl" aria-hidden="true">⚠️</div><h2 className="mt-3 text-lg font-extrabold text-red-700">Reservation not allowed</h2><p className="mt-2 text-sm text-slate-600">{eligibility.reason}</p><a href={studentPortal.routes.catalog} className="mt-5 inline-block rounded-xl border border-slate-300 bg-white px-5 py-3 text-sm font-bold text-slate-700">Return to catalog</a></div>}</div></section></div></StudentLayout>;
}

function Detail({ label, value }) { return <div><dt className="text-[10px] font-bold uppercase tracking-wide text-slate-400">{label}</dt><dd className="mt-1 text-sm font-bold text-slate-800">{value || 'N/A'}</dd></div>; }
